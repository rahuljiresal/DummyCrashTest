'use strict';

let Colors = require('../colors');

module.exports = {
    container: {
        flex: 1,
        flexDirection: 'column',
        alignItems: 'stretch'
    },
    label: {
        fontSize: 12,
        color: Colors.lightTextColor,
    }
};